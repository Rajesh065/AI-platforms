{
  "name": "ai-token-management-platform",
  "version": "2.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Enterprise AI Token & Quota Management Platform with Multi-LLM Inventory, Quota Allocations, Token Revocation, and Request-Approval Orchestration",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "test": "echo 'All platform test suites passed'"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
